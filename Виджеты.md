---
tags:
  - netology
---
### Класс QWidget

- Класс QWidget - базовый для всех объектов пользовательского интерфейса
- QWidget унаследован от класса QObject, а значит - может использовать механизм сигналов\слотов и механизм объектной иерархии

### Конструктор QWidget

- Класс QWidget и большинство унаследованных от него классов имеют конструктор с двумя параметрами
- Первый параметр - указатель на родительский виджет. Если он равен нулю, то этот виджет верхний в иерархии
- Второй параметр служит для задания свойств окна. С его помощью можно управлять внешним видом окна и режимом отображения: чтобы окно не перекрывалось другими окнами и т.д.

### Методы QWidget

- setEnabled() - устанавливает виджет в доступное (enabled) или недоступное (disabled)
состояние
- mousePressEvent(), mouseMoveEvent(), mouseReleaseEvent() или mouseDoubleClickEvent() -
реагирование на события мыши
- setGeometry() - позволяет изменить размеры и геометрию виджета. move() и resize()
изменяют их по отдельности
- setCursor() - позволяет изменить курсор мыши на виджете
- setMinimumSize(), setMaximumSize() - устанавливают минимальные и максимальные 
размеры виджета


### Элементы ввода

- Line Edit
- Text Edit
- Plain Text Edit
- Spin Box
- Double Spin Box


### Qline Edit

однострочное текстовое поле

Основные методы:
- text()
- textChanged()
- textEdited
- setReadOnly()
- setText()
- setEchoMode()


### QTextEdit

редактор текса

Основные методы:
- QLineEdit
- append()
- find()


### QSpinBox

счетчик

Основные методы:
- setRange() 
- value()
- setValue()
- valueChanged()


### Другие элементы ввода

- QDateTimeEdit - элемент ввода даты
- QDoubleSpinBox - счетчик для ввода чисел double
- QValidator - класс, позволяющий проверить ввод данных в виджеты


### Элементы отображения
### QListWidget

виджет списка

Основные методы:
- addItem()
- clear()
- insertItems() или insertItem()
- currentItem()
- setSelectionModel()
- sortItems()


### Qlabel

виджет надписи

Основные методы:
- setText()
- setAlignment()


### QProgressBar

индикатор выполнения

Основные методы:
- setOrientation()
- setMinimum() и setMaximum()
- setValue()
- format()


### Кнопки
### Виды кнопок

- Normal Button (обычная кнопка) 
- Toggle Button (выключатель)
- Flat Button (плоская кнопка)
- Pixmap Button (кнопка с изображением)

Сигналы кнопок:
- clicked()
- pressed()
- released()
- toggled()

Опросы состояния кнопок:
- isDown()
- isChecked()


### Frames

рамки используются для вложения и группировки виджетов, а также для декорирования

Свойства фреймов:
- frameShape
- frameShadow
- lineWidth
- midLineWidth


### Group Box

групповые поля обычно используются для группировки коллекций флажков и радиокнопок с аналогичными целями


### Tab Widgets

виджеты с вкладками позволяют разработчику разбивать содержимое виджета на различные помеченные секции, только одна из которых отображается в любой момент времени


### Другие элементы
### QCheckBox

флажок для выбора

Основные методы:
- setCheckState()
- setTristate()
- stateChanged()